#include "main.h"

/**
 * _abs - Function to compute abs of a number
 * @n: n is a number to be checked
 *
 * Return: Abs of number
 */
int print_sign(int n)
{
	int return = 0;

	if (n <0)
	n = -1 * n;
	return (n);
}
