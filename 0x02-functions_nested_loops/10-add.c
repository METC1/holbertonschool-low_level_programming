#include "main.h"

/**
 * add - Function to add 2 int numbers
 * @a: first int number
 * @b: second int number
 *
 * Return: Int addition of two numbers
 */
int add(int a, int b)
{
	int added = 0;

	added = a + b;
	return (added);
}
